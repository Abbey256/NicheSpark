# Copy this to .env.local and fill in after deploying the SAM stack
# The API Gateway URL is printed as output when you run: sam deploy

VITE_API_URL=https://YOUR_API_GATEWAY_ID.execute-api.us-east-1.amazonaws.com/prod
